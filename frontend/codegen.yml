overwrite: true
schema: ${NEXT_PUBLIC_GRAPHQL_HTTP_URL}
documents: 'src/graphql/**/*.graphql'
generates:
  src/types/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
      withComponent: false
      withHOC: false
